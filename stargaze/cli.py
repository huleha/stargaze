"""Command-line interface."""

# stargaze --near whereabouts --within radius --head direction
# for example:
# stargaze --near Krakow --within 30km --head south
# `within` argument should understand units of length, `pint` module can be used
# for conversion
# `head` argument should understand textual directions such as south, west,
# north, easth, s, w, n, e, sw, etc. and numeric azimuth


def main():
    pass


if __name__ == '__main__':
    main()
